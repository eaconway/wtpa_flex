import {connect} from 'react-redux';
import LeftSidebarModal from './left_sidebar_modal';

const mapStateToProps = state => {
  return {

  };
};

const mapDispatchToProps = dispatch => {
  return {

  };
};

export default connect(mapStateToProps, mapDispatchToProps)(LeftSidebarModal);